// import { fromJS } from 'immutable';
// import { makeSelectTodosDomain } from '../selectors';

// const selector = makeSelectTodosDomain();

describe('makeSelectTodosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
